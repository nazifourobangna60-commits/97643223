import { GardenStyle, Sunlight, GardenSize } from './types';

export const GARDEN_STYLES = Object.values(GardenStyle);
export const SUNLIGHT_OPTIONS = Object.values(Sunlight);
export const SIZE_OPTIONS = Object.values(GardenSize);

export const SAMPLE_PROMPT = "I want a relaxing space with a water feature and bamboo.";
